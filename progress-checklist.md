# Project Progress Checklist

- [x] Task 1: Remove `MeetingPreparation` models from `clients` and `clients_le`
- [x] Task 2: Update `BankingRelationship` and `LE_BankingRelationship` models with JSONFields and new choices
- [x] Task 3: Update `Product` and `LE_Product` models with new fields and verbose names
- [x] Task 4: Update `registration.py` in `clients` and `clients_le` to reflect new fields and removed models
- [x] Task 5: Run `makemigrations` and `migrate`
- [x] Verification and testing
